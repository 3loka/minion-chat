# Packer variables (all are required)
region                    = "us-east-1"
dockerhub_id              = "srahul3"

# Terraform variables (all are required)
ami                       = "ami-05f879114bfea7934"

name_prefix               = "minion"
response_service_count    = 2